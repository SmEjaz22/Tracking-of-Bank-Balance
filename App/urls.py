from django.contrib import admin
from django.urls import path
from . import views

app_name = "Bank"

urlpatterns = [
    # path('Dashboard/', views.dashboard, name='Dashboard'),
    # path('Totals/<uuid:uuid>', views.get_total, name='Totals'),
    # path('change_salary/<uuid:uuid>', views.change_salary, name='ChangeSalary'),
    # path('change_saving/<uuid:uuid>', views.change_saving, name='ChangeSaving'),
    # path('change_others/<uuid:uuid>', views.change_others, name='ChangeOthers'),

]

